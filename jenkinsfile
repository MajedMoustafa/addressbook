pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
      	echo 'execution tests'
        sh 'sudo catalina.sh run'
        sh 'sudo curl localhost:8080/addressbook-2.0/'
      }
    }
  }
}
