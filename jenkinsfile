pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          maven --version
          git --version
          curl --version
        '''
      }
    }
  }
}
